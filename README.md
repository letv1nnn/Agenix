# Agenix
Intern Operator Project focusing on Agent Identity

## Project Structure

The operator code lives in the `agenix-operator/` subdirectory. When adding or editing GitHub Actions workflows, set `working-directory: agenix-operator` and use `go-version-file: agenix-operator/go.mod`.
